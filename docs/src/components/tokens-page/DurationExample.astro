---
interface Props {
  name: string;
}

const { name } = Astro.props;
---

<div class="cd-card duration-example" x-data="{ clicked: false }">
  <p class="cd-card-title">duration-{name}</p>
  <slot />
  <label
    class="wrapper"
    style={{ transitionDuration: `var(--cd-duration-${name})` }}
  >
    <input type="checkbox" x-model="clicked" />
    Click or tap
  </label>
</div>

<style>
  .wrapper {
    display: flex;
    align-items: center;
    justify-content: center;
    inline-size: 10rem;
    block-size: 10rem;
    border-radius: var(--cd-radius-m);
    border: var(--cd-border-s) solid var(--cd-color-blue-7);
    box-shadow: var(--cd-shadow-s);
    background-color: var(--cd-color-blue-8);
    color: var(--cd-color-blue-2);
    font-size: 1rem;
    font-weight: var(--cd-font-weight-bold);
    box-sizing: border-box;
    overflow: clip;
    position: relative;
    transition-property: background-color, border-color, color, outline;
    transition-timing-function: var(--cd-ease-default);
    user-select: none;
    outline: 0 solid transparent;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
  }

  .wrapper input {
    appearance: none;
    height: 100%;
    width: 100%;
    position: absolute;
    inset: 0;
    opacity: 0;
    outline: 0;
    cursor: pointer;
  }

  .wrapper:has(input:checked) {
    background-color: var(--cd-color-red-8);
    border-color: var(--cd-color-red-7);
    color: var(--cd-color-red-2);
  }

  .wrapper:has(input:focus-visible) {
    outline: var(--cd-border-m) solid var(--cd-color-blue-7);
  }

  .wrapper:has(input:checked:focus-visible) {
    outline: var(--cd-border-m) solid var(--cd-color-red-7);
  }
</style>

<style is:global>
  @keyframes progress {
    0% {
      inline-size: 0%;
    }
    100% {
      inline-size: 100%;
    }
  }
</style>
