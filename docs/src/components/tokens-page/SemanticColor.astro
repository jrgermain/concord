---
interface Props {
  name: string;
  description: string;
}

const { name, description } = Astro.props;

const backgroundColor = `var(--cd-color-${name})`;
---

<div class="semantic-color">
  <div class="swatch" style={{ backgroundColor }}></div>
  <strong class="name">color-{name}</strong>
  <div class="description">{description}</div>
</div>

<style>
  .semantic-color {
    background-color: var(--cd-color-surface-primary);
    padding: var(--cd-space-s);
    border-radius: var(--cd-radius-m);
    border: var(--cd-border-s) solid var(--cd-color-border);
    display: grid;
    grid-template-columns: auto 1fr;
    grid-template-rows: auto auto;
    gap: 0 var(--cd-space-xs);
    place-items: center start;
  }

  .swatch {
    height: 2rem;
    width: 2rem;
    border-radius: var(--cd-radius-s);
    border: var(--cd-border-s) solid var(--cd-color-border);
    grid-row: 1;
    grid-column: 1;
  }

  .name {
    text-wrap: nowrap;
    grid-row: 1;
    grid-column: 2;
    margin-inline-end: auto;
  }

  .description {
    grid-row: 2;
    grid-column: 1 / span 2;
    margin-block-start: var(--cd-space-2xs);
  }
</style>
