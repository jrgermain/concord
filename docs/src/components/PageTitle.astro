---
interface Props {
  title: string;
  subtitle?: string;
}

const { title, subtitle } = Astro.props;
---

<style>
  .page-title {
    margin-bottom: var(--cd-space-m);
  }

  .page-title h1 {
    margin: 0;
  }

  .page-title p {
    color: var(--cd-color-text-secondary);
    font-size: var(--cd-font-size-l);
    margin-top: var(--cd-space-3xs);
  }
</style>
<div class="page-title">
  <h1 class="cd-heading cd-level-1">{title}</h1>
  {subtitle && <p>{subtitle}</p>}
</div>
