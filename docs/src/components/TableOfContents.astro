---
interface Props {
  showHeading?: boolean;
  areItemsClickable?: boolean;
  contents: {
    title: string;
    description: string;
    slug: string;
  }[];
}

const { showHeading = true, areItemsClickable = true, contents } = Astro.props;

let headingClass = "cd-heading cd-level-2 toc-heading";
if (!showHeading) {
  headingClass += " cd-visually-hidden";
}
---

<style>
  .toc-heading {
    font-size: var(--cd-font-size-xl);
    margin-block-end: var(--cd-space-xs);
  }

  .toc-card-container {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(min(100%, 25rem), 1fr));
    gap: var(--cd-space-s);
  }
</style>
<section>
  <h2 class={headingClass}>Table of Contents</h2>
  <div class="toc-card-container">
    {
      contents.map((entry) => (
        <div class="cd-card">
          <div class="cd-card-title">
            {areItemsClickable ? (
              <a href={`./${entry.slug}`}>{entry.title}</a>
            ) : (
              entry.title
            )}
          </div>
          <p>{entry.description}</p>
        </div>
      ))
    }
  </div>
</section>
